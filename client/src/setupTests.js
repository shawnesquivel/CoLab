import "@testing-library/jest-dom";
// const mockNoop = () => new Promise(() => {});

// jest.mock("axios", () => ({
//   default: mockNoop,
//   get: mockNoop,
//   post: mockNoop,
//   put: mockNoop,
//   delete: mockNoop,
//   patch: mockNoop,
// }));
