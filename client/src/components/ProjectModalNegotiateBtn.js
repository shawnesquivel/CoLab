import React from "react";

const ProjectModalNegotiateBtn = ({ onClick }) => {
  return (
    <button
      className="form__btn form__btn-next form__btn--light"
      onClick={onClick}
    >
      Negotiate
    </button>
  );
};

export default ProjectModalNegotiateBtn;
